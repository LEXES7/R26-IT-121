# Copy to `.env` and adjust. `.env` is gitignored.

# FastAPI service config. Port 8001 is what the fusion engine expects —
# see fusion_engine/DeepSentinel/config.example.ini, [upstream].
BEHAVIORAL_API_HOST=0.0.0.0
BEHAVIORAL_API_PORT=8001
